#ifndef FUNCTION_HPP
#define FUNCTION_HPP

#include <cmath>
#include <numeric>
#include <algorithm>
#include <iostream>
#include <vector>

void fillVector(std::vector<int>&);
void sumElements(std::vector<int>&);
void sizeVector(std::vector<int>&);
void seekNumber(std::vector<int>&);
void comparisons(std::vector<int>&);
void replaceZeros(std::vector<int>);
void interval(std::vector<int>&);
void replacEvenNum(std::vector<int>&);
void deleteRepeat(std::vector<int>&);

#endif // Function.hpp

